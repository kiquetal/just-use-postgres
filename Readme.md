### Just Use Postgres!!

### Indexes

- [Chapter I Connecting with psql](chapterone.md): Instructions on how to connect to the Postgres database using `psql` directly or through Docker Compose, including connection strings and passwords. It also covers generating mock data using `generate_series` and lists useful `psql` commands.
